---
import { getEntry } from 'astro:content';
import Layout from '../components/Layout.astro';
const entry = await getEntry('pages', 'index'); // Dateiname ohne .md
const { Content } = await entry.render();
---
<Layout title="Faelis' Paw Artistry — Art & Commissions">
  <Content />
</Layout>

