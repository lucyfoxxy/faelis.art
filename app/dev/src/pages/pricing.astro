---
import { getEntry } from 'astro:content';
import Layout from '@Design/Layout.astro';
import PricingPage from '@Pages/PricingPage.astro';

const title = 'Pricing';
const catalog = (await getEntry('categories', 'albums')).data;
---
<Layout {title}>
  <PricingPage catalog={catalog} />
</Layout>
